<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Page</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
     display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f0f0f0;
    }

    .container {
      display: flex;
      width: 800px;
      height: 450px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .login-form {
      flex: 1;
      background-color: #fff;
      padding: 50px;
    }

    .login-form h2 {
      font-size: 24px;
      margin-bottom: 20px;
      color: #333;
    }

    .input-field {
      margin-bottom: 20px;
    }

    .input-field input {
      width: 100%;
      padding: 15px;
      border: none;
      border-radius: 5px;
      background-color: #f0f0f0;
      font-size: 16px;
    }

    .forgot-password {
      display: block;
      margin-bottom: 20px;
      font-size: 14px;
      text-align: right;
      color: #888;
      text-decoration: none;
    }

    .forgot-password:hover {
      text-decoration: underline;
    }

    .signin-btn {
      width: 100%;
      padding: 15px;
      border: none;
      border-radius: 5px;
      background: linear-gradient(90deg, #f00, #e60000);
      color: #fff;
      font-size: 16px;
      cursor: pointer;
    }

    .signin-btn:hover {
      background: linear-gradient(90deg, #e60000, #cc0000);
    }

    .login-welcome {
      flex: 1;
      background-image: url('/images/kjjj.jpg'); /* Replace with your image URL */
      background-size: cover;
      background-position: center;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      color: #fff;
    }

    .login-welcome h2 {
      font-size: 24px;
      margin-bottom: 50px;
    }

    .signup-section {
      text-align: center;
    }

    .signup-btn {
      background-color: rgba(255, 255, 255, 0.2);
      border: none;
      padding: 15px 40px;
      border-radius: 30px;
      color: #fff;
      font-size: 16px;
      cursor: pointer;
    }

    .signup-btn:hover {
      background-color: rgba(255, 255, 255, 0.4);
    }

    .signup-section p {
      margin-top: 10px;
      font-size: 14px;
    }
  </style>
  <!-- Link to the JavaScript file -->
  <script src="loginpg.js" defer></script>
</head>
<body>
  <div class="container">
    <div class="login-form">
      <h2>Log In</h2>
      <form id="loginForm" action="/login" method="post">
        <div class="input-field">
          <input type="text" id="username" name="userName" placeholder="Username">
        </div>
        <div class="input-field">
          <input type="password" id="password" name="password" placeholder="Password">
        </div>
        <button type="submit" class="signin-btn">Sign In</button>
      </form>
    </div>
    <div class="login-welcome">
      <h2>Welcome To login</h2>
      <div class="signup-section">
        <a href="/signin"><button class="signup-btn">Sign up</button></a>
        <p>Don't Have account</p>
      </div>
    </div>
  </div>
</body>
</html>